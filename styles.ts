import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create ({
  betterButton: {
    borderRadius: 8,
    paddingTop: 12,
    paddingRight: 22,
    paddingBottom: 12,
    paddingLeft: 22,
    margin: 18,
    borderWidth: 1,
  },
  buttonTitle: {
    textAlign: 'center',
  },
})